---
phase: 03-tab-management
plan: 02
type: execute
wave: 1
depends_on: ["00"]
files_modified:
  - entrypoints/content/TabGrid.tsx
  - tests/components/TabGrid.test.tsx
autonomous: true
requirements:
  - MNGT-04
user_setup: []
must_haves:
  truths:
    - "Bag shows a persistent search input at the top of the panel"
    - "Search matches title or URL with case-insensitive substring logic"
    - "Search resets each time the bag is reopened"
    - "No-results state is distinct from the true empty-bag state"
  artifacts:
    - path: "entrypoints/content/TabGrid.tsx"
      provides: "Search-enabled grid container"
      min_lines: 100
  key_links:
    - from: "TabGrid.tsx"
      to: "savedTabs"
      via: "derived filtered view"
      pattern: "savedTabs\\.(getValue|watch)"
---

# Plan 03-02: Search and Filtered Grid View

**Objective:** Add a persistent search control to the bag and filter visible cards by title or URL without changing stored data.

**Purpose:** Make saved tabs findable with the smallest practical UI change, matching the user's phase-3 direction.

**Output:** Updated `TabGrid.tsx` with local query state, derived filtered results, and focused tests.

## Context

**@$HOME/.claude/get-shit-done/workflows/execute-plan.md**
**@$HOME/.claude/get-shit-done/templates/summary.md**

@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tab-management/03-CONTEXT.md
@.planning/phases/03-tab-management/03-UI-SPEC.md
@.planning/phases/03-tab-management/03-VALIDATION.md

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: Render the search field and local query state</name>
  <files>entrypoints/content/TabGrid.tsx, tests/components/TabGrid.test.tsx</files>
  <action>
Add a top search field inside `TabGrid`. Keep `query` local to the component and do not auto-focus it when the bag opens.
  </action>
  <verify>
    <automated>pnpm test --run tests/components/TabGrid.test.tsx -t "search"</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Filter cards by title or URL</name>
  <files>entrypoints/content/TabGrid.tsx, tests/components/TabGrid.test.tsx</files>
  <action>
Use a derived filtered array with case-insensitive substring matching against `title` and `url`. Do not debounce in the first implementation.
  </action>
  <verify>
    <automated>pnpm test --run tests/components/TabGrid.test.tsx -t "filter"</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reset search on reopen and show the correct empty state</name>
  <files>entrypoints/content/TabGrid.tsx, tests/components/TabGrid.test.tsx</files>
  <action>
Reset the query whenever `isOpen` transitions to `true`. Preserve the existing empty-bag state and add a distinct no-results state for filtered-empty cases.
  </action>
  <verify>
    <automated>pnpm test --run tests/components/TabGrid.test.tsx</automated>
  </verify>
</task>

