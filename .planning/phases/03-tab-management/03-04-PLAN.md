---
phase: 03-tab-management
plan: 04
type: execute
wave: 3
depends_on: ["00", "01", "02", "03"]
files_modified:
  - entrypoints/content/ConfirmDialog.tsx
  - entrypoints/content/TabGrid.tsx
  - tests/components/ConfirmDialog.test.tsx
  - tests/components/TabGrid.test.tsx
autonomous: true
requirements:
  - MNGT-02
user_setup: []
must_haves:
  truths:
    - "Clear-all is a single obvious action in the bag header"
    - "Bulk deletion always requires confirmation"
    - "Cancel and confirm branches are both covered"
  artifacts:
    - path: "entrypoints/content/ConfirmDialog.tsx"
      provides: "Focused confirmation dialog for clear-all"
      min_lines: 50
  key_links:
    - from: "TabGrid.tsx"
      to: "ConfirmDialog.tsx"
      via: "isOpen/onConfirm/onCancel props"
      pattern: "ConfirmDialog"
---

# Plan 03-04: Clear-All Confirmation Flow

**Objective:** Add a bulk clear action that is visible in the bag header and protected by a confirmation dialog.

**Purpose:** Give users a fast cleanup tool without making accidental data loss too easy.

**Output:** Clear-all button, confirmation dialog, and confirmation/cancel tests.

## Context

**@$HOME/.claude/get-shit-done/workflows/execute-plan.md**
**@$HOME/.claude/get-shit-done/templates/summary.md**

@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tab-management/03-CONTEXT.md
@.planning/phases/03-tab-management/03-UI-SPEC.md
@.planning/phases/03-tab-management/03-VALIDATION.md

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: Create ConfirmDialog component</name>
  <files>entrypoints/content/ConfirmDialog.tsx, tests/components/ConfirmDialog.test.tsx</files>
  <action>
Implement a small, phase-local confirm dialog with explicit confirm/cancel actions, keyboard-accessible markup, and no generic modal abstraction.
  </action>
  <verify>
    <automated>pnpm test --run tests/components/ConfirmDialog.test.tsx</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add clear-all action to TabGrid header</name>
  <files>entrypoints/content/TabGrid.tsx, tests/components/TabGrid.test.tsx</files>
  <action>
Render the `清空` action in the grid header. Hide or disable it when there are no saved tabs. Opening it should show the confirmation dialog, not immediately clear data.
  </action>
  <verify>
    <automated>pnpm test --run tests/components/TabGrid.test.tsx -t "clear"</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire confirm/cancel behavior</name>
  <files>entrypoints/content/TabGrid.tsx, tests/components/TabGrid.test.tsx</files>
  <action>
On confirm, call `clearTabs()` and return the panel to the true empty state. On cancel, leave saved data unchanged and close the dialog.
  </action>
  <verify>
    <automated>pnpm test --run tests/components/TabGrid.test.tsx tests/components/ConfirmDialog.test.tsx</automated>
  </verify>
</task>

