---
phase: 03-tab-management
plan: 01
type: execute
wave: 1
depends_on: ["00"]
files_modified:
  - utils/storage.ts
  - tests/storage.test.ts
autonomous: true
requirements:
  - MNGT-01
  - MNGT-02
  - MNGT-03
user_setup: []
must_haves:
  truths:
    - "Storage exposes explicit helpers for clear-all and reorder"
    - "Reorder persistence does not mutate item identity"
    - "Existing add/remove behavior remains intact"
  artifacts:
    - path: "utils/storage.ts"
      provides: "Saved-tab helpers for add, remove, clear, reorder"
      exports: ["addTab", "removeTab", "clearTabs", "reorderTabs"]
  key_links:
    - from: "utils/storage.ts"
      to: "savedTabs storage item"
      via: "getValue/setValue"
      pattern: "savedTabs\\.(getValue|setValue)"
---

# Plan 03-01: Storage Helpers for Management Actions

**Objective:** Extend the saved-tab storage layer with explicit helpers for bulk clear and persistent reorder.

**Purpose:** Keep bag management logic simple in UI code by centralizing data mutation in `utils/storage.ts`.

**Output:** Updated storage helpers with focused tests.

## Context

**@$HOME/.claude/get-shit-done/workflows/execute-plan.md**
**@$HOME/.claude/get-shit-done/templates/summary.md**

@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tab-management/03-CONTEXT.md
@.planning/phases/03-tab-management/03-RESEARCH.md
@.planning/phases/03-tab-management/03-VALIDATION.md

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: Add clear-all helper</name>
  <files>utils/storage.ts, tests/storage.test.ts</files>
  <action>
Add `clearTabs(): Promise<void>` that sets `savedTabs` to an empty array. Cover it with a storage test that verifies existing data is removed.
  </action>
  <verify>
    <automated>pnpm test --run tests/storage.test.ts -t "clear"</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add reorder helper</name>
  <files>utils/storage.ts, tests/storage.test.ts</files>
  <action>
Add `reorderTabs(nextTabs: SavedTab[]): Promise<void>` that persists a full ordered array. Keep it intentionally simple: no schema changes, no duplicate generation, no timestamp rewriting.
  </action>
  <verify>
    <automated>pnpm test --run tests/storage.test.ts -t "reorder"</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Guard existing behavior</name>
  <files>tests/storage.test.ts</files>
  <action>
Retain coverage proving:
- `addTab` still prevents duplicate URLs
- newly collected tabs are still prepended newest-first
- `removeTab` still removes a single URL
  </action>
  <verify>
    <automated>pnpm test --run tests/storage.test.ts</automated>
  </verify>
</task>

