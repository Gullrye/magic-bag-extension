---
phase: 03-tab-management
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/setup.ts
  - tests/storage.test.ts
  - tests/components/TabGrid.test.tsx
  - tests/components/TabCard.test.tsx
  - tests/components/ConfirmDialog.test.tsx
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Phase 3 test scaffolding exists before feature implementation"
    - "Drag-and-drop tests have the environment shims they need"
    - "Search, delete, clear-all, and reorder all have Wave 0 test placeholders"
  artifacts:
    - path: "tests/components/ConfirmDialog.test.tsx"
      provides: "Stub coverage for the clear-all confirmation dialog"
      min_lines: 20
  key_links:
    - from: "tests/setup.ts"
      to: "@dnd-kit-based tests"
      via: "pointer/drag shims"
      pattern: "PointerEvent|setPointerCapture|releasePointerCapture"
---

# Plan 03-00: Phase 3 Test Scaffolding

**Objective:** Extend the existing test environment so Phase 3 features can be built test-first without blocking on drag, dialog, or search test setup.

**Purpose:** Reuse the working Phase 2 Vitest foundation, then add only the extra scaffolding needed for management interactions.

**Output:** Updated test setup plus Wave 0 test placeholders for search, delete, clear-all, and reorder.

## Context

**@$HOME/.claude/get-shit-done/workflows/execute-plan.md**
**@$HOME/.claude/get-shit-done/templates/summary.md**

@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tab-management/03-CONTEXT.md
@.planning/phases/03-tab-management/03-RESEARCH.md
@.planning/phases/03-tab-management/03-UI-SPEC.md
@.planning/phases/03-tab-management/03-VALIDATION.md

## Tasks

<task type="auto">
  <name>Task 1: Extend test environment for management interactions</name>
  <files>tests/setup.ts</files>
  <read_first>
    - tests/setup.ts
    - .planning/phases/03-tab-management/03-RESEARCH.md
  </read_first>
  <action>
Add only the missing jsdom shims required for Phase 3 interaction tests. Prioritize:
- `PointerEvent` support for drag tests
- `setPointerCapture` / `releasePointerCapture` no-op shims if needed
- lightweight dialog-related helpers only if the chosen confirm implementation requires them

Do not rebuild the existing Chrome API mock structure.
  </action>
  <verify>
    <automated>pnpm test --run tests/setup.ts</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 2: Add Wave 0 placeholders for storage and grid tests</name>
  <files>tests/storage.test.ts, tests/components/TabGrid.test.tsx</files>
  <read_first>
    - .planning/phases/03-tab-management/03-VALIDATION.md
  </read_first>
  <action>
Extend existing test files with pending or placeholder coverage for:
- clearing all tabs
- reordering tab arrays
- title/URL search filtering
- resetting search on reopen
- empty vs no-results states
  </action>
  <verify>
    <automated>pnpm test --run tests/storage.test.ts tests/components/TabGrid.test.tsx</automated>
  </verify>
</task>

<task type="auto">
  <name>Task 3: Add Wave 0 placeholders for destructive UI tests</name>
  <files>tests/components/TabCard.test.tsx, tests/components/ConfirmDialog.test.tsx</files>
  <read_first>
    - .planning/phases/03-tab-management/03-UI-SPEC.md
  </read_first>
  <action>
Extend `tests/components/TabCard.test.tsx` for delete affordance behavior and create `tests/components/ConfirmDialog.test.tsx` for confirm/cancel flows.
  </action>
  <verify>
    <automated>pnpm test --run tests/components/TabCard.test.tsx tests/components/ConfirmDialog.test.tsx</automated>
  </verify>
</task>

