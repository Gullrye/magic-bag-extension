---
phase: 03-tab-management
plan: 05
type: execute
wave: 4
depends_on: ["00", "01", "02", "03", "04"]
files_modified:
  - package.json
  - pnpm-lock.yaml
  - entrypoints/content/TabGrid.tsx
  - tests/components/TabGrid.test.tsx
  - tests/storage.test.ts
autonomous: true
requirements:
  - MNGT-03
user_setup: []
must_haves:
  truths:
    - "Grid supports drag-to-reorder with a movement threshold"
    - "Reorder does not make click-to-open unreliable"
    - "New order persists after storage updates"
  artifacts:
    - path: "entrypoints/content/TabGrid.tsx"
      provides: "Sortable tab grid using @dnd-kit"
      min_lines: 140
  key_links:
    - from: "package.json"
      to: "@dnd-kit packages"
      via: "dependencies"
      pattern: "@dnd-kit"
    - from: "TabGrid.tsx"
      to: "utils/storage.ts"
      via: "reorderTabs helper"
      pattern: "reorderTabs"
---

# Plan 03-05: Persistent Drag-to-Reorder

**Objective:** Add reliable grid reordering with @dnd-kit and persist the updated order through the storage helper layer.

**Purpose:** Finish Phase 3 management with a maintained drag-and-drop solution that fits the current card grid and extension UI.

**Output:** Installed DnD dependencies, sortable grid behavior, and persistence tests.

## Context

**@$HOME/.claude/get-shit-done/workflows/execute-plan.md**
**@$HOME/.claude/get-shit-done/templates/summary.md**

@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tab-management/03-CONTEXT.md
@.planning/phases/03-tab-management/03-RESEARCH.md
@.planning/phases/03-tab-management/03-UI-SPEC.md
@.planning/phases/03-tab-management/03-VALIDATION.md

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: Install and verify @dnd-kit dependencies</name>
  <files>package.json, pnpm-lock.yaml</files>
  <action>
Add `@dnd-kit/core`, `@dnd-kit/sortable`, and `@dnd-kit/utilities` to the project. Keep dependency scope narrow; do not introduce alternative drag libraries.
  </action>
  <verify>
    <automated>pnpm install --lockfile-only</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add sortable behavior to TabGrid</name>
  <files>entrypoints/content/TabGrid.tsx, tests/components/TabGrid.test.tsx</files>
  <action>
Wrap visible cards in `DndContext` and `SortableContext`, use a pointer activation distance around 8px, and keep click-to-open behavior intact when the user is not dragging.
  </action>
  <verify>
    <automated>pnpm test --run tests/components/TabGrid.test.tsx -t "reorder"</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Persist the new order</name>
  <files>entrypoints/content/TabGrid.tsx, tests/storage.test.ts, tests/components/TabGrid.test.tsx</files>
  <action>
On drag end, compute the new visible order and persist the full saved-tab order via `reorderTabs()`. Preserve non-matching tabs correctly if the user reorders while a search query is active, or explicitly disable reorder while filtered if that proves safer.
  </action>
  <verify>
    <automated>pnpm test --run tests/storage.test.ts tests/components/TabGrid.test.tsx</automated>
  </verify>
</task>
