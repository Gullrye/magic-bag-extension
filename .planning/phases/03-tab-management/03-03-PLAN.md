---
phase: 03-tab-management
plan: 03
type: execute
wave: 2
depends_on: ["00", "01", "02"]
files_modified:
  - entrypoints/content/TabCard.tsx
  - entrypoints/content/TabGrid.tsx
  - tests/components/TabCard.test.tsx
  - tests/components/TabGrid.test.tsx
autonomous: true
requirements:
  - MNGT-01
user_setup: []
must_haves:
  truths:
    - "Each card exposes an explicit delete affordance"
    - "Delete click never triggers tab open"
    - "Deleting from a filtered view updates the visible list correctly"
  artifacts:
    - path: "entrypoints/content/TabCard.tsx"
      provides: "Tab card with delete affordance and open behavior"
      min_lines: 60
  key_links:
    - from: "TabCard.tsx"
      to: "TabGrid.tsx"
      via: "delete callback prop"
      pattern: "onDelete|handleDelete"
---

# Plan 03-03: Individual Delete Flow

**Objective:** Add a clear per-card delete action without regressing click-to-open behavior.

**Purpose:** Let users remove a single saved tab directly from the bag while keeping the interaction explicit and low-risk.

**Output:** Updated card UI and grid wiring for individual deletion with test coverage.

## Context

**@$HOME/.claude/get-shit-done/workflows/execute-plan.md**
**@$HOME/.claude/get-shit-done/templates/summary.md**

@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-tab-management/03-CONTEXT.md
@.planning/phases/03-tab-management/03-UI-SPEC.md
@.planning/phases/03-tab-management/03-VALIDATION.md

## Tasks

<task type="auto" tdd="true">
  <name>Task 1: Add delete affordance to TabCard</name>
  <files>entrypoints/content/TabCard.tsx, tests/components/TabCard.test.tsx</files>
  <action>
Add an icon-first delete button with a 44x44px hit target and `aria-label="删除标签页"`. Stop propagation so the card open handler does not fire when deleting.
  </action>
  <verify>
    <automated>pnpm test --run tests/components/TabCard.test.tsx -t "delete"</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire delete through TabGrid</name>
  <files>entrypoints/content/TabGrid.tsx, tests/components/TabGrid.test.tsx</files>
  <action>
Pass an `onDelete(url)` callback from `TabGrid` into each card and invoke `removeTab(url)` through the storage helper flow.
  </action>
  <verify>
    <automated>pnpm test --run tests/components/TabGrid.test.tsx -t "delete"</automated>
  </verify>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Verify filtered deletion behavior</name>
  <files>tests/components/TabGrid.test.tsx</files>
  <action>
Cover the case where the grid is filtered, one matching item is deleted, and the remaining visible state updates correctly.
  </action>
  <verify>
    <automated>pnpm test --run tests/components/TabGrid.test.tsx</automated>
  </verify>
</task>

