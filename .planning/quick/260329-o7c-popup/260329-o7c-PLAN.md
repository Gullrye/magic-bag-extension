---
phase: quick
plan: 260329-o7c
type: execute
wave: 1
depends_on: []
files_modified:
  - entrypoints/popup/PopupPage.tsx
  - entrypoints/popup/style.css
autonomous: true
requirements: [QUICK-o7c]

must_haves:
  truths:
    - "Popup layout is compact with no leftover spacing from removed text"
    - "No orphan CSS classes from previous text elements remain"
    - "All functional elements (buttons, tooltips, locale switcher) still work"
  artifacts:
    - path: "entrypoints/popup/PopupPage.tsx"
      provides: "Cleaned-up popup component"
    - path: "entrypoints/popup/style.css"
      provides: "Cleaned-up popup styles with no orphan rules"
  key_links:
    - from: "entrypoints/popup/PopupPage.tsx"
      to: "entrypoints/popup/style.css"
      via: "classNames"
      pattern: "className="
---

<objective>
Clean up popup layout after tooltip conversion.

Purpose: The previous task (260329-ngz) converted body text paragraphs to tooltip icons. Now there are orphan CSS classes, unnecessary wrapper divs, and spacing tuned for text that no longer exists. This plan removes dead code and tightens the layout.

Output: A more compact popup with no leftover artifacts from the old text-heavy layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@entrypoints/popup/PopupPage.tsx
@entrypoints/popup/style.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove orphan CSS and simplify hero wrapper</name>
  <files>entrypoints/popup/PopupPage.tsx, entrypoints/popup/style.css</files>
  <action>
In PopupPage.tsx:
1. Remove the `popup-page__hero-copy` wrapper div around eyebrow + title (lines 235 and 247 closing div). Move the eyebrow `<p>` and title `<h1>` directly into `popup-page__hero-top`. The wrapper was needed to control width alongside the lead text which no longer exists.
2. Remove the `popup-page__eyebrow` paragraph (line 236). The extension name is redundant in the popup -- the user just opened it, they know what extension this is. This saves a line of vertical space.

In style.css:
3. Delete the `.popup-page__hero-copy` rule block (lines 61-64). No longer referenced.
4. Delete the `.popup-page__lead` rule block (lines 84-89). The lead text was moved to tooltip in the previous task; this class is orphaned.
5. Delete the `.popup-card__body--muted` rule block (lines 244-247). Never used in the current TSX.
6. Reduce `.popup-page__hero` padding from `14px` to `12px` (line 46). Less content means less padding needed.
7. Reduce `.popup-page__hero-actions` margin-top from `10px` to `8px` (line 125). Tighter after eyebrow removal.
8. Reduce `.popup-page__title` margin from `6px 0 4px` to `4px 0 0` (line 77). Tighter without eyebrow above it.

Do NOT change any button styles, tooltip styles, or the card layout structure. Only remove dead code and tighten spacing in the hero area.
  </action>
  <verify>
    <automated>cd /Users/gullrye/Documents/ai/tag-extension && pnpm build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - No orphan CSS classes (popup-page__hero-copy, popup-page__lead, popup-card__body--muted) exist in style.css
    - popup-page__hero-copy wrapper div removed from TSX
    - popup-page__eyebrow removed from TSX (ext name no longer shown)
    - Hero section has tighter padding and margins
    - Build succeeds with no errors
  </done>
</task>

</tasks>

<verification>
1. `pnpm build` passes with no errors
2. `grep -c "popup-page__hero-copy\|popup-page__lead\|popup-card__body--muted" entrypoints/popup/style.css` returns 0
3. `grep "popup-page__hero-copy\|popup-page__eyebrow" entrypoints/popup/PopupPage.tsx` returns nothing
</verification>

<success_criteria>
Popup builds cleanly with no orphan styles or wrappers from the pre-tooltip layout. Hero section is visually more compact.
</success_criteria>

<output>
After completion, create `.planning/quick/260329-o7c-popup/260329-o7c-SUMMARY.md`
</output>
