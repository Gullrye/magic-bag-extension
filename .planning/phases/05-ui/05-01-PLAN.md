---
phase: 05-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - entrypoints/content/TabCard.tsx
  - entrypoints/content/TabGrid.tsx
  - tests/components/TabCard.test.tsx
  - tests/components/TabGrid.test.tsx
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Cards display at 140x90px size (reduced from 160x100px)"
    - "Delete button is hidden by default and appears on card hover"
    - "Delete button fades in/out smoothly over 150ms"
    - "Header (search + clear button) stays visible when scrolling the grid"
    - "Existing hover effects (scale, border, shadow) remain unchanged"
    - "Delete button touch target remains 44x44px for accessibility"
  artifacts:
    - path: "entrypoints/content/TabCard.tsx"
      provides: "Tab card component with smaller size and hover-only delete"
      min_lines: 80
      contains: "w-[140px] h-[90px]"
    - path: "entrypoints/content/TabGrid.tsx"
      provides: "Grid container with sticky header"
      min_lines: 250
      contains: "sticky top-0"
  key_links:
    - from: "TabCard.tsx"
      to: "delete button visibility"
      via: "CSS group-hover or React state"
      pattern: "group-hover:opacity-100|isHovered"
    - from: "TabGrid.tsx"
      to: "header container"
      via: "sticky CSS positioning"
      pattern: "sticky top-0 z-10 bg-amber-50"
---

<objective>
Optimize the tab panel UI with smaller cards (140x90px), hover-only delete button, and sticky header for improved density and usability.

Purpose: Increase visible card count and reduce visual clutter while keeping search accessible during scroll.
Output: Modified TabCard.tsx and TabGrid.tsx with updated dimensions and interactions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-ui/05-CONTEXT.md
@.planning/phases/05-ui/05-UI-SPEC.md

<interfaces>
<!-- Key types and contracts from existing codebase -->

From entrypoints/content/types.ts:
```typescript
export interface SavedTab {
  url: string;
  title: string;
  favicon?: string;
  timestamp: number;
}
```

From entrypoints/content/TabCard.tsx:
```typescript
interface TabCardProps {
  tab: SavedTab;
  onClick: (url: string) => void;
  onDelete?: (url: string) => void;
}
```

From entrypoints/content/TabGrid.tsx:
```typescript
interface TabGridProps {
  isOpen: boolean;
  onClose: () => void;
  onTabClick: (url: string) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reduce card size and add hover-only delete button</name>
  <files>entrypoints/content/TabCard.tsx, tests/components/TabCard.test.tsx</files>
  <read_first>
    - entrypoints/content/TabCard.tsx (current implementation)
    - tests/components/TabCard.test.tsx (existing tests)
    - .planning/phases/05-ui/05-UI-SPEC.md (design specification)
  </read_first>
  <behavior>
    - Card dimensions: 160x100px -> 140x90px
    - Delete button: visible always -> hidden by default, visible on hover
    - Delete button transition: none -> 150ms ease-out for opacity and visibility
    - Touch target: 44x44px unchanged
    - All other styling unchanged (padding, favicon size, title, hover effects)
  </behavior>
  <action>
Modify TabCard.tsx to implement D-01, D-02, D-10 from CONTEXT.md:

1. **Card size change (D-01):**
   - Change `w-[160px] h-[100px]` to `w-[140px] h-[90px]` in the outer div className

2. **Delete button hover visibility (D-02, D-10):**
   - Add `group` class to the outer card div
   - Modify the delete button className to include:
     - `opacity-0` and `invisible` (or `visibility-hidden`) for default state
     - `group-hover:opacity-100` and `group-hover:visible` for hover state
     - `transition-[opacity,visibility] duration-150 ease-out` for smooth fade

3. **Implementation using CSS group-hover (preferred over React state for simplicity):**
   - The card wrapper gets `group` class
   - The delete button gets the visibility classes

Example change for delete button:
```tsx
className="absolute right-1 top-1 flex h-11 w-11 items-start justify-end rounded-full p-2 text-red-600/80 transition-[opacity,visibility,color] duration-150 ease-out opacity-0 invisible group-hover:opacity-100 group-hover:visible hover:text-red-600 focus-visible:text-red-600"
```

Update tests in TabCard.test.tsx to verify:
- Delete button exists but is hidden by default (opacity-0 or check computed style)
- The group class is present on the card wrapper
</action>
  <verify>
    <automated>npm test -- --filter=TabCard --run</automated>
  </verify>
  <acceptance_criteria>
    - Card div has class `w-[140px] h-[90px]`
    - Card div has class `group`
    - Delete button has class `opacity-0`
    - Delete button has class `invisible`
    - Delete button has class `group-hover:opacity-100`
    - Delete button has class `group-hover:visible`
    - Delete button has transition classes with `duration-150`
    - All existing tests pass
  </acceptance_criteria>
  <done>
    TabCard displays at 140x90px, delete button is hidden by default and fades in on hover with 150ms transition.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add sticky header to TabGrid</name>
  <files>entrypoints/content/TabGrid.tsx, tests/components/TabGrid.test.tsx</files>
  <read_first>
    - entrypoints/content/TabGrid.tsx (current implementation)
    - tests/components/TabGrid.test.tsx (existing tests)
    - .planning/phases/05-ui/05-UI-SPEC.md (design specification)
  </read_first>
  <behavior>
    - Header container (search + clear button) stays fixed at top when scrolling
    - Header has amber-50 background to match panel
    - Header has z-10 to stay above scrollable content
    - Visual spacing preserved with pb-6 padding
    - Panel remains scrollable via overflow-auto
  </behavior>
  <action>
Modify TabGrid.tsx to implement D-05, D-06, D-07 from CONTEXT.md:

1. **Sticky header implementation:**
   - Find the header div with `className="mb-6 flex items-center gap-3"`
   - Wrap it with a sticky container div OR add sticky classes directly
   - Changes needed:
     - Add `sticky top-0 z-10 bg-amber-50` to the header container
     - Keep `pb-6` for spacing (or adjust mb-6 to pb-6)
     - The parent scroll container already has `overflow-auto`

2. **Implementation approach:**
   - Change `<div className="mb-6 flex items-center gap-3">` to:
     `<div className="sticky top-0 z-10 bg-amber-50 pb-6 flex items-center gap-3">`
   - This keeps the header sticky at top while content scrolls beneath

3. **Verify scrolling behavior:**
   - The panel already has `overflow-auto` on line 159
   - Sticky header will work within this scroll container

Update tests in TabGrid.test.tsx if needed to verify:
- Header container has sticky classes
- Consider adding a test that verifies header stays in viewport (optional, visual verification is acceptable)
</action>
  <verify>
    <automated>npm test -- --filter=TabGrid --run</automated>
  </verify>
  <acceptance_criteria>
    - Header div has class `sticky`
    - Header div has class `top-0`
    - Header div has class `z-10`
    - Header div has class `bg-amber-50`
    - Header div has class `pb-6` (replacing or in addition to mb-6)
    - All existing tests pass
  </acceptance_criteria>
  <done>
    TabGrid header (search input and clear button) stays visible at top when scrolling through tabs.
  </done>
</task>

</tasks>

<verification>
After implementation:
1. Run `npm test` to verify all tests pass
2. Run `pnpm dev` and manually verify:
   - Cards are smaller (140x90px vs 160x100px)
   - Delete button is hidden by default, appears on card hover
   - Delete button fades in/out smoothly
   - Header stays visible when scrolling the grid
   - All existing hover effects still work (scale, border, shadow)
</verification>

<success_criteria>
- Cards display at 140x90px (measurable in browser dev tools)
- Delete button is hidden when not hovering (opacity-0, invisible)
- Delete button appears with 150ms fade on card hover
- Header stays fixed at top when scrolling content
- All existing tests pass
- No visual regressions in panel styling
</success_criteria>

<output>
After completion, create `.planning/phases/05-ui/05-01-SUMMARY.md`
</output>
