# Quick Task 260328-pa0 Plan

## Goal

Add a file-sync flow for tag management without replacing the existing import/export flow.

## Tasks

1. Add a lightweight sync-file helper
   - files: `utils/fileSync.ts`
   - action: persist a chosen JSON file handle and provide bind/read/write helpers using the File System Access API
   - verify: helper can bind a file, read tab arrays, and write current saved tabs back to the same file
   - done: popup can reuse a single helper for sync-file actions

2. Add sync-file actions to popup
   - files: `entrypoints/popup/PopupPage.tsx`, `entrypoints/popup/style.css`, `utils/i18n.ts`
   - action: add a compact sync section with bind/import/export actions while keeping existing import/export intact
   - verify: popup shows sync status, importing merges tabs, exporting writes current tabs
   - done: user can bind a JSON file and manually sync against it from popup

3. Add automated coverage and quick-task bookkeeping
   - files: `tests/components/PopupPage.test.tsx`, `.planning/STATE.md`, `.planning/quick/260328-pa0-json/*`
   - action: cover sync actions in tests and record the quick task in planning artifacts
   - verify: test suite and build stay green
   - done: sync flow is tested and tracked
