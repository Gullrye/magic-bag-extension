---
phase: 02-tab-collection-display
plan: 00
type: execute
wave: 0
depends_on: []
files_modified:
  - tests/setup.ts
  - tests/background.test.ts
  - tests/storage.test.ts
  - tests/tabs.test.ts
  - tests/utils/clickOutside.test.ts
autonomous: true
requirements: []
user_setup: []
must_haves:
  truths:
    - "Test infrastructure supports background script testing"
    - "Test infrastructure supports storage layer testing"
    - "Test infrastructure supports component testing"
    - "Chrome API mocks are comprehensive and reset between tests"
    - "Vitest configuration supports all test types"
  artifacts:
    - path: "tests/setup.ts"
      provides: "Global test setup with Chrome API mocks and React Testing Library"
      min_lines: 50
    - path: "tests/background.test.ts"
      provides: "Stub tests for background script functionality"
      min_lines: 30
    - path: "tests/storage.test.ts"
      provides: "Stub tests for storage operations"
      min_lines: 30
    - path: "tests/tabs.test.ts"
      provides: "Stub tests for grid rendering and favicon handling"
      min_lines: 30
    - path: "tests/utils/clickOutside.test.ts"
      provides: "Stub tests for click-outside detection"
      min_lines: 20
  key_links:
    - from: "tests/setup.ts"
      to: "vitest config"
      via: "setupFiles configuration"
      pattern: "setupFiles.*tests/setup"
    - from: "All test files"
      to: "tests/setup.ts"
      via: "global beforeEach hooks"
      pattern: "beforeEach\\(\\)"
---

# Plan 02-00: Test Infrastructure Setup

**Objective:** Establish comprehensive test infrastructure with Chrome API mocks, React Testing Library setup, and stub test files for all Phase 2 functionality.

**Purpose:** Create the foundation for automated testing that validates Nyquist compliance. Wave 0 ensures all subsequent implementation tasks have automated verification ready.

**Output:** Complete test setup with mocks, stub tests for all features, and vitest configuration.

## Context

**@$HOME/.claude/get-shit-done/workflows/execute-plan.md**
**@$HOME/.claude/get-shit-done/templates/summary.md**

@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-tab-collection-display/02-CONTEXT.md
@.planning/phases/02-tab-collection-display/02-RESEARCH.md
@.planning/phases/02-tab-collection-display/02-VALIDATION.md

## Interfaces

<!-- From Phase 1 test pattern (01-02-SUMMARY.md) -->
```typescript
// Existing test setup pattern from Phase 1
import { vi } from 'vitest';
import { cleanup } from '@testing-library/react';

// Cleanup after each test
afterEach(() => {
  cleanup();
});

// Chrome API mocks
(globalThis as any).chrome = {
  storage: {
    local: {
      get: vi.fn(),
      set: vi.fn(),
    },
  },
};
```

<!-- From 02-VALIDATION.md Wave 0 Requirements -->
```
Required test stubs:
- entrypoints/background.test.ts — COLL-01 (context menu registration)
- tests/storage.test.ts — COLL-04 (tab storage operations)
- tests/tabs.test.ts — GRID-02, GRID-03 (grid rendering, favicons)
- tests/utils/clickOutside.test.ts — GRID-05 (click-outside detection)
```

## Tasks

<task type="auto">
  <name>Task 1: Extend tests/setup.ts with Chrome API mocks</name>
  <files>tests/setup.ts</files>
  <read_first>
    - tests/setup.ts (existing setup from Phase 1)
    - .planning/phases/02-tab-collection-display/02-RESEARCH.md (Chrome Extension APIs section)
  </read_first>
  <action>
Extend tests/setup.ts to include comprehensive Chrome API mocks for Phase 2:

```typescript
import { vi } from 'vitest';
import { cleanup } from '@testing-library/react';

// Cleanup after each test
afterEach(() => {
  cleanup();
});

// Reset all mocks before each test
beforeEach(() => {
  vi.clearAllMocks();
});

// Chrome storage mock (from Phase 1)
const mockStorage = new Map<string, any>();

(globalThis as any).chrome = {
  runtime: {
    onInstalled: {
      addListener: vi.fn(),
    },
    getManifest: vi.fn(() => ({ name: '法宝袋', version: '1.0.0' })),
  },
  contextMenus: {
    create: vi.fn(),
    onClicked: {
      addListener: vi.fn(),
    },
  },
  tabs: {
    query: vi.fn(),
    create: vi.fn(),
    remove: vi.fn(),
    sendMessage: vi.fn(),
  },
  storage: {
    local: {
      get: vi.fn((keys: string | string[] | null) =>
        Promise.resolve(
          keys === null
            ? Object.fromEntries(mockStorage)
            : typeof keys === 'string'
            ? { [keys]: mockStorage.get(keys) }
            : Object.fromEntries(
                Array.from(mockStorage.entries()).filter(([k]) =>
                  Array.isArray(keys) ? keys.includes(k) : true
                )
              )
        )
      ),
      set: vi.fn((items: { [key: string]: any }) => {
        Object.entries(items).forEach(([k, v]) => mockStorage.set(k, v));
        return Promise.resolve();
      }),
      clear: vi.fn(() => {
        mockStorage.clear();
        return Promise.resolve();
      }),
    },
  },
};
```

This provides mocks for:
- chrome.runtime (extension lifecycle)
- chrome.contextMenus (context menu registration and handling)
- chrome.tabs (tab manipulation)
- chrome.storage.local (data persistence)
  </action>
  <verify>
    <automated>pnpm test --run tests/setup.test.ts</automated>
  </verify>
  <done>
    - tests/setup.ts exports comprehensive Chrome API mocks
    - All Chrome APIs used in Phase 2 have working mocks
    - Mocks reset between tests via beforeEach
    - React Testing Library cleanup configured
  </done>
</task>

<task type="auto">
  <name>Task 2: Create stub test files for all Phase 2 features</name>
  <files>tests/background.test.ts, tests/storage.test.ts, tests/tabs.test.ts, tests/utils/clickOutside.test.ts</files>
  <read_first>
    - .planning/phases/02-tab-collection-display/02-VALIDATION.md (Wave 0 Requirements)
  </read_first>
  <action>
Create stub test files that will be filled in by subsequent implementation plans:

**tests/background.test.ts:**
```typescript
import { describe, it, expect } from 'vitest';

describe('Background Script', () => {
  describe('Context Menu Registration (COLL-01)', () => {
    it('should register context menu on install', () => {
      // TODO: Implement in plan 02-02
      expect(true).toBe(true);
    });

    it('should have correct menu title', () => {
      // TODO: Implement in plan 02-02
      expect(true).toBe(true);
    });
  });

  describe('Tab Collection Handler', () => {
    it('should capture tab data on context menu click', () => {
      // TODO: Implement in plan 02-02
      expect(true).toBe(true);
    });

    it('should close original tab after collection', () => {
      // TODO: Implement in plan 02-02
      expect(true).toBe(true);
    });
  });
});
```

**tests/storage.test.ts:**
```typescript
import { describe, it, expect } from 'vitest';

describe('Storage Layer', () => {
  describe('Tab Storage Operations (COLL-04)', () => {
    it('should store tab data', () => {
      // TODO: Implement in plan 02-01
      expect(true).toBe(true);
    });

    it('should prevent duplicate URLs', () => {
      // TODO: Implement in plan 02-01
      expect(true).toBe(true);
    });

    it('should maintain newest-first order', () => {
      // TODO: Implement in plan 02-01
      expect(true).toBe(true);
    });
  });
});
```

**tests/tabs.test.ts:**
```typescript
import { describe, it, expect } from 'vitest';

describe('Tab Grid and Cards', () => {
  describe('Grid Rendering (GRID-02)', () => {
    it('should render tabs in grid layout', () => {
      // TODO: Implement in plan 02-04
      expect(true).toBe(true);
    });

    it('should handle empty state', () => {
      // TODO: Implement in plan 02-04
      expect(true).toBe(true);
    });
  });

  describe('Favicon Handling (GRID-03)', () => {
    it('should display favicon when available', () => {
      // TODO: Implement in plan 02-03
      expect(true).toBe(true);
    });

    it('should show fallback icon when favicon missing', () => {
      // TODO: Implement in plan 02-03
      expect(true).toBe(true);
    });
  });
});
```

**tests/utils/clickOutside.test.ts:**
```typescript
import { describe, it, expect } from 'vitest';

describe('useClickOutside Hook', () => {
  describe('Click Detection (GRID-05)', () => {
    it('should call callback when clicking outside', () => {
      // TODO: Implement in plan 02-04
      expect(true).toBe(true);
    });

    it('should not call callback when clicking inside', () => {
      // TODO: Implement in plan 02-04
      expect(true).toBe(true);
    });

    it('should cleanup event listener on unmount', () => {
      // TODO: Implement in plan 02-04
      expect(true).toBe(true);
    });
  });
});
```

All stub tests use TODO comments referencing the plan that will implement them.
  </action>
  <verify>
    <automated>pnpm test --run tests/background.test.ts tests/storage.test.ts tests/tabs.test.ts tests/utils/clickOutside.test.ts</automated>
  </verify>
  <done>
    - tests/background.test.ts created with COLL-01 stubs
    - tests/storage.test.ts created with COLL-04 stubs
    - tests/tabs.test.ts created with GRID-02, GRID-03 stubs
    - tests/utils/clickOutside.test.ts created with GRID-05 stubs
    - All stubs reference implementation plans in TODOs
    - All stub tests pass (placeholder expects)
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify vitest configuration and update VALIDATION.md</name>
  <files>vitest.config.ts, .planning/phases/02-tab-collection-display/02-VALIDATION.md</files>
  <read_first>
    - vitest.config.ts (existing configuration)
    - .planning/phases/02-tab-collection-display/02-VALIDATION.md
  </read_first>
  <action>
Verify vitest.config.ts has proper configuration:

```typescript
import { defineConfig } from 'vitest/config';
import react from '@vitejs/plugin-react';
import path from 'path';

export default defineConfig({
  plugins: [react()],
  test: {
    globals: true,
    environment: 'jsdom',
    setupFiles: ['./tests/setup.ts'],
    coverage: {
      provider: 'v8',
      reporter: ['text', 'json', 'html'],
      exclude: [
        'node_modules/',
        'tests/',
        '**/*.test.{ts,tsx}',
        '**/*.config.{ts,js}',
        'wxt.config.ts',
      ],
    },
  },
  resolve: {
    alias: {
      '~': path.resolve(__dirname, './'),
    },
  },
});
```

Update .planning/phases/02-tab-collection-display/02-VALIDATION.md frontmatter:
```yaml
---
phase: 2
slug: tab-collection-display
status: draft
nyquist_compliant: true  # Changed from false
wave_0_complete: true    # Changed from false
created: 2026-03-25
---
```

This marks Wave 0 as complete and Nyquist compliance achieved.
  </action>
  <verify>
    <automated>pnpm test --run</automated>
  </verify>
  <done>
    - vitest.config.ts configured with jsdom environment and setupFiles
    - tests/setup.ts referenced in configuration
    - 02-VALIDATION.md updated with nyquist_compliant: true
    - 02-VALIDATION.md updated with wave_0_complete: true
    - All tests run successfully
  </done>
</task>

## Verification

**Infrastructure Verification:**
1. `pnpm test --run` — All stub tests pass
2. `npx tsc --noEmit` — TypeScript compilation succeeds
3. `cat .planning/phases/02-tab-collection-display/02-VALIDATION.md | grep nyquist_compliant` — Returns `nyquist_compliant: true`

## Success Criteria

- ✅ Comprehensive Chrome API mocks in tests/setup.ts
- ✅ Stub test files for all Phase 2 features
- ✅ Vitest configuration supports all test types
- ✅ All stub tests pass
- ✅ Nyquist compliance achieved (wave_0_complete: true)
- ✅ VALIDATION.md updated to reflect Wave 0 completion

## Output

After completion, create `.planning/phases/02-tab-collection-display/02-00-SUMMARY.md`

---

*Phase: 02-tab-collection-display*
*Plan: 00*
*Created: 2026-03-25*
