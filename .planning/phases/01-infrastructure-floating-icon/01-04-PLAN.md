---
phase: 01-infrastructure-floating-icon
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified: [.output/chrome-mv3/manifest.json, README.md, entrypoints/content/BagIcon.tsx]
autonomous: false
requirements: [ICON-03]
user_setup: []
must_haves:
  truths:
    - "Icon position persists after page reload"
    - "Icon position persists after browser restart"
    - "Icon displays correctly on multiple websites with CSS isolation"
    - "Extension loads without errors in Edge browser"
  artifacts:
    - path: ".output/chrome-mv3/manifest.json"
      provides: "Final extension manifest for loading"
      contains: "manifest_version: 3, all permissions, content_scripts"
    - path: "README.md"
      provides: "Setup and testing instructions"
      contains: "build, load, test instructions"
    - path: "entrypoints/content/BagIcon.tsx"
      provides: "Final BagIcon component with persistence"
      contains: "position persistence logic, storage integration"
  key_links:
    - from: "extension"
      to: "chrome.storage.local"
      via: "WXT storage module"
      pattern: "iconPosition\\.setValue"
    - from: "BagIcon component"
      to: "storage"
      via: "useEffect load on mount"
      pattern: "iconPosition\\.getValue"
---

<objective>
Verify position persistence, perform cross-site testing, and finalize Phase 1 documentation.

Purpose: Validate that ICON-03 (position persists across page reloads and browser sessions) works correctly through manual testing. This is the gate that ensures the floating icon infrastructure is complete and ready for Phase 2 tab collection features.

Output: Verified working extension with position persistence, cross-site CSS isolation confirmed, and README documentation for loading/testing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/01-infrastructure-floating-icon/01-CONTEXT.md
@.planning/phases/01-infrastructure-floating-icon/01-RESEARCH.md
@.planning/phases/01-infrastructure-floating-icon/01-UI-SPEC.md

Locked decisions to implement:
- D-09: Position stored per-domain in chrome.storage.local (survives page reloads and browser restarts)
- ICON-03: Icon position persists across page reloads and browser sessions

Research guidance:
- Test on complex websites (Gmail, Facebook) to ensure CSS isolation works (RESEARCH.md Pitfall 1)
- Verify position survives page navigation and browser restart (RESEARCH.md Pitfall 3)
- Use chrome://extensions for loading and debugging (RESEARCH.md Development Tools)

Phase 1 success criteria (from ROADMAP.md):
1. Extension installs and loads without errors in Edge browser
2. Floating icon appears on all web pages with proper Shadow DOM isolation
3. User can drag icon to any of 4 screen edges (top/bottom/left/right)
4. Icon position persists after page reload and browser restart
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build final extension and verify manifest</name>
  <files>.output/chrome-mv3/manifest.json, .output/chrome-mv3/content.js, .output/chrome-mv3/background.js</files>
  <read_first>
    - /Users/gullrye/Documents/ai/tag-extension/wxt.config.ts (configuration to verify)
    - /Users/gullrye/Documents/ai/tag-extension/package.json (build scripts)
  </read_first>
  <action>
Build the final extension and verify all artifacts are generated correctly:

1. Run production build:
```bash
cd /Users/gullrye/Documents/ai/tag-extension
pnpm build
```

2. Verify output directory structure:
```bash
ls -la .output/chrome-mv3/
```

3. Verify manifest.json contains all required fields:
- manifest_version: 3
- permissions: ['storage', 'activeTab', 'contextMenus', 'tabs', 'unlimitedStorage']
- host_permissions: ['<all_urls>']
- background.service_worker
- content_scripts with matches: ['<all_urls>']
- action (popup) configuration

4. Verify bundled files exist:
- content.js (content script bundle with BagIcon component)
- background.js (service worker)
- asset files (if any)

5. Check file sizes are reasonable (content.js should be < 500KB for this simple phase)

This validates INFR-01 (Extension boots with WXT + React + TypeScript + Manifest V3) and ensures the build is ready for manual testing.
</action>
  <verify>
    <automated>
```bash
cd /Users/gullrye/Documents/ai/tag-extension
test -f .output/chrome-mv3/manifest.json && echo "manifest.json exists"
test -f .output/chrome-mv3/content.js && echo "content.js exists"
test -f .output/chrome-mv3/background.js && echo "background.js exists"
grep -q '"manifest_version":\s*3' .output/chrome-mv3/manifest.json && echo "Manifest V3 confirmed"
grep -q '"unlimitedStorage"' .output/chrome-mv3/manifest.json && echo "unlimitedStorage found"
grep -q '"storage"' .output/chrome-mv3/manifest.json && echo "storage permission found"
grep -q '"contextMenus"' .output/chrome-mv3/manifest.json && echo "contextMenus permission found"
grep -q '"matches":\s*\["<all_urls>"\]' .output/chrome-mv3/manifest.json && echo "all_urls content script found"
```
    </automated>
  </verify>
  <done>
Extension built successfully with:
- Valid Manifest V3 (.output/chrome-mv3/manifest.json)
- All 5 permissions present (storage, activeTab, contextMenus, tabs, unlimitedStorage)
- Content script bundle (.output/chrome-mv3/content.js)
- Service worker bundle (.output/chrome-mv3/background.js)
- No build errors or warnings
  </done>
</task>

<task type="auto">
  <name>Task 2: Create README with setup and testing instructions</name>
  <files>README.md</files>
  <read_first>
    - /Users/gullrye/Documents/ai/tag-extension/.planning/PROJECT.md (project vision and core value)
    - /Users/gullrye/Documents/ai/tag-extension/.planning/ROADMAP.md (phase overview)
  </read_first>
  <action>
Create README.md with setup and testing instructions:

```markdown
# 法宝袋 (Magic Bag)

一款 Edge 浏览器扩展，提供一个可拖拽的悬浮图标"法宝袋"。用户可以将浏览器标签页"收入"袋中，标签像棋子一样在棋盘上铺开展示。

**Core Value:** 一键收纳标签页，让浏览器保持清爽。

## Phase 1: Infrastructure & Floating Icon

This phase establishes the extension infrastructure and floating draggable icon.

### Setup

1. **Install dependencies:**
\`\`\`bash
pnpm install
\`\`\`

2. **Build the extension:**
\`\`\`bash
pnpm build
\`\`\`

3. **Load in Edge:**
   - Open Edge and navigate to \`edge://extensions\`
   - Enable "Developer mode" (toggle in top-right)
   - Click "Load unpacked"
   - Select the \`magic-bag/.output/chrome-mv3\` directory

### Testing

**Smoke Test:**
1. Visit any website (e.g., https://example.com)
2. Verify the floating icon appears in the bottom-right corner
3. Verify the icon is a 48x48px gray circle with shadow

**Drag Test:**
1. Drag the icon to each edge (top, right, bottom, left)
2. Release within 50px of an edge
3. Verify the icon snaps to that edge
4. Verify visual feedback during drag (blue color, enhanced shadow, cursor change)

**Persistence Test:**
1. Drag icon to a new position
2. Reload the page (F5)
3. Verify icon position is preserved
4. Restart Edge browser
5. Verify icon position is still preserved

**CSS Isolation Test:**
1. Visit complex websites (Gmail, Facebook, Reddit)
2. Verify icon appearance is consistent across all sites
3. Inspect element and verify Shadow DOM isolation
4. Verify host page CSS doesn't affect the icon

### Tech Stack

- **WXT 0.20.20** - Extension build framework
- **React 19.2.4** - UI framework
- **TypeScript 5.x** - Type safety
- **Tailwind CSS** - Styling (inside Shadow DOM)
- **react-draggable 4.5.0** - Drag behavior
- **Manifest V3** - Chrome extension standard

### Architecture

- \`entrypoints/background.ts\` - Service worker for privileged APIs
- \`entrypoints/content/index.tsx\` - Content script with Shadow DOM UI
- \`entrypoints/content/BagIcon.tsx\` - Floating draggable icon component
- \`utils/storage.ts\` - WXT storage module for position persistence
- \`utils/drag.ts\` - Edge-snapping drag utilities

### Current Status

**Phase 1 (Infrastructure & Floating Icon):** ✅ Complete
- [x] WXT + React + TypeScript setup
- [x] Shadow DOM style isolation
- [x] Draggable floating icon
- [x] Edge snapping behavior
- [x] Position persistence

**Next Phase:** Tab Collection & Display (Phase 2)

## Development

\`\`\`bash
# Development mode with HMR
pnpm dev

# Type checking
pnpm tsc --noEmit

# Build for production
pnpm build
\`\`\`

## License

MIT
```

Key documentation elements:
- Project name and core value (from PROJECT.md)
- Phase 1 scope (from ROADMAP.md)
- Setup instructions for loading unpacked extension
- Testing checklist covering all Phase 1 requirements
- Tech stack with versions (from RESEARCH.md)
- Architecture overview with file purposes
- Development commands
</action>
  <verify>
    <automated>
```bash
cd /Users/gullrye/Documents/ai/tag-extension
test -f README.md && echo "README.md exists"
grep -q "法宝袋" README.md && echo "project name found"
grep -q "Setup" README.md && echo "setup section found"
grep -q "Testing" README.md && echo "testing section found"
grep -q "edge://extensions" README.md && echo "edge extensions URL found"
grep -q "pnpm build" README.md && echo "build command found"
grep -q "Shadow DOM" README.md && echo "Shadow DOM mentioned"
grep -q "WXT.*0\\.20" README.md && echo "WXT version found"
```
    </automated>
  </verify>
  <done>
README.md created with:
- Project name and core value
- Setup instructions (install, build, load)
- Testing checklist (smoke, drag, persistence, CSS isolation)
- Tech stack with versions
- Architecture overview
- Development commands
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 1 extension with infrastructure, floating icon, drag behavior, and position persistence</what-built>
  <how-to-verify>
Follow these steps to verify Phase 1 is complete:

1. **Load Extension:**
   - Open Microsoft Edge
   - Navigate to `edge://extensions`
   - Enable "Developer mode" (top-right toggle)
   - Click "Load unpacked"
   - Select `/Users/gullrye/Documents/ai/tag-extension/.output/chrome-mv3`

2. **Verify Installation:**
   - Check "法宝袋" appears in extensions list
   - Verify no errors in extension card
   - Check permissions: storage, activeTab, contextMenus, tabs, unlimitedStorage

3. **Test Icon Display (ICON-01):**
   - Open https://example.com
   - Verify gray circular icon appears in bottom-right corner
   - Check icon is 48x48px with shadow
   - Verify icon is above all page content (z-index)

4. **Test Drag Behavior (ICON-02):**
   - Drag icon to top edge → release → verify snaps to top
   - Drag icon to right edge → release → verify snaps to right
   - Drag icon to bottom edge → release → verify snaps to bottom
   - Drag icon to left edge → release → verify snaps to left
   - Drag to middle → release → verify stays where released
   - Verify visual feedback during drag (blue color, cursor: grabbing)

5. **Test Position Persistence (ICON-03):**
   - Drag icon to top-left corner
   - Reload page (F5)
   - Verify icon still in top-left
   - Close Edge, reopen, visit example.com
   - Verify icon position persisted across browser restart

6. **Test CSS Isolation (INFR-02):**
   - Visit https://gmail.com (complex site)
   - Verify icon appearance is consistent
   - Right-click icon → Inspect
   - Verify #shadow-root (open) exists (Shadow DOM isolation)
   - Visit https://facebook.com
   - Verify icon still looks correct

7. **Verify Build:**
   - Run `pnpm build` in terminal
   - Confirm zero errors and warnings

**Expected Result:** All 7 tests pass without issues.
  </how-to-verify>
  <resume-signal>Type "approved" if all tests pass, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
Overall phase verification:

1. **Build Verification**: `pnpm build` exits 0 with no errors
2. **Manifest Verification**: .output/chrome-mv3/manifest.json is valid with all permissions
3. **Type Safety**: `npx tsc --noEmit` confirms no TypeScript errors
4. **Manual Testing**: User confirms all 7 tests pass (icon display, drag, persistence, CSS isolation)
5. **Documentation**: README.md provides clear setup and testing instructions

Phase 1 success criteria (from ROADMAP.md):
- [ ] Extension installs and loads without errors in Edge browser
- [ ] Floating icon appears on all web pages with proper Shadow DOM isolation
- [ ] User can drag icon to any of 4 screen edges (top/bottom/left/right)
- [ ] Icon position persists after page reload and browser restart
</verification>

<success_criteria>
- [ ] Extension builds to .output/chrome-mv3/ without errors
- [ ] Manifest V3 with all 5 permissions present
- [ ] Floating icon appears on all web pages (tested on 3+ sites)
- [ ] Drag behavior works smoothly to all 4 edges
- [ ] Edge snapping activates within 50px threshold
- [ ] Position persists after page reload
- [ ] Position persists after browser restart
- [ ] CSS isolation confirmed (Shadow DOM present, styles consistent)
- [ ] README.md documents setup and testing
- [ ] User approval from checkpoint verification
</success_criteria>

<output>
After completion, create `.planning/phases/01-infrastructure-floating-icon/01-04-SUMMARY.md`
</output>
